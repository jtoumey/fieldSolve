#ifndef GRID_H
#define GRID_H

class Grid
{
private:
    int dimension;
    int num_elements;


public:
    Grid(int dim_, int num_elem_) : dimension(dim_), num_elements(num_elem_) {} ;

};

#endif // GRID_H
